export interface CreateHospital {
    name: string;
    code: string;
    city: string;
  }
  
  export interface Hospital {
    id: string;
    name: string;
    code: string;
    city: string;
  }
  