export interface CreatePatient {
    code: string;
    firstName: string;    
    lastName: string;
  }
  
  export interface Patient {
    id: string;
    code: string;
    firstName: string;    
    lastName: string;
  }
  